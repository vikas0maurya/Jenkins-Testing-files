pipeline {
  agent any
  stages {
    stage('plan') {
      steps {
        echo 'to plan the project'
      }
    }

    stage('code') {
      steps {
        echo 'to code the application'
      }
    }

    stage('build') {
      steps {
        echo 'to build the code'
      }
    }

  }
}